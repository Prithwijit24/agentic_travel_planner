apiVersion: v1
kind: Service
metadata:
  name: agentic-travel-planner-api
spec:
  selector:
    app: agentic-travel-planner-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP

